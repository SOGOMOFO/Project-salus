# Salus Kernel v0.9 Plan

## Objective
Integrate the Kernel orchestration pipeline into the Command UI.

## Must Ship
- /command/kernel interface upgrade
- UI form for orchestration input
- display context packet, response plan, execution gate, doctrine check, and learning recommendation
- tests
