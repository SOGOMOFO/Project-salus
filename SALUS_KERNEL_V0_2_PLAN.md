# Salus Kernel v0.2 Plan

## Objective
Connect the Kernel to real Salus subsystems.

## Must Ship
- Kernel reads Core Identity status
- Kernel exposes subsystem registry
- Kernel can identify available modules
- Kernel routing maps intent to actual subsystem routes
- Tests preserve all existing behavior
