# Salus Kernel v0.3 Plan

## Objective
Build a real context packet that combines identity, memory, knowledge, and judgment routing.

## Must Ship
- context packet service
- /api/kernel/context route
- context includes identity status, selected subsystem, and request classification
- tests
